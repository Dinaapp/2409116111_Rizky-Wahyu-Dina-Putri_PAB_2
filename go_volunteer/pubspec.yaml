name: govolunteer
description: Aplikasi Relawan Kegiatan Sosial Samarinda

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  uuid: ^4.2.1
  intl: ^0.20.2
  provider: ^6.1.1
  cupertino_icons: ^1.0.6
  supabase_flutter: ^2.3.4
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  generate: true
  assets:
    - .env